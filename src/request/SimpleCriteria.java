package request;

/**
 * All possible simple criteria
 * A simple criteria is just composed of one element, non simple criteria have their own class.
 */
public enum SimpleCriteria implements Criteria {
	CULTURE;
}
